import htmlIncludeAll from "./rLib-htmlInclude.js";
import showDropdownMenu from "./rLib-dropdownMenu.js";
import { galleryLoad, galleryShift } from "./rLib-gallery.js";

const rLib = {
  htmlIncludeAll,
  showDropdownMenu,
  galleryLoad,
  galleryShift,
};

export default rLib;
