import { htmlInclude, htmlIncludeAll } from "./rLib-htmlInclude.js";
import showDropdownMenu from "./rLib-dropdownMenu.js";
import { loadImagesFromFolder, showImage } from "./rLib-slideshow.js";

const rLib = {
  htmlInclude,
  htmlIncludeAll,
  showDropdownMenu,
  loadImagesFromFolder,
  showImage,
};

export default rLib;
